# ELM — 1️⃣🐉🐎

## Installation

Claude:

```bash
mkdir -p .claude/skills && git clone https://github.com/jysh1214/elm.git .claude/skills/elm && rm -rf .claude/skills/elm/.git
```

Auggie:

```bash
mkdir -p .augment/skills && git clone https://github.com/jysh1214/elm.git .augment/skills/elm && rm -rf .augment/skills/elm/.git
```

## Usage

```bash
ELM review {notes}
```
